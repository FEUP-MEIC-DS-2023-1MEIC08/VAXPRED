version: '3'

services:
  frontend:
    container_name: vaxpred_frontend
    build:
      context: .
    ports:
      - "4200:4200"
    depends_on:
      - backend

  backend:
    container_name: vaxpred_backend
    build:
      context: ./api
    ports:
      - "8000:8000"
